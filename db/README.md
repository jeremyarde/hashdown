## Setup

1. Setup supabase
   1. supabase cli
2. Use ormlite to generate sql based on rust structs
   1. put migrations into supabase/migrations/<time><migration name>.sql


1. rust structs
2. ormlite cli -> migration scripts
   1. or use supabase and diff command





cargo install diesel_cli --no-default-features --features postgres
cargo install diesel_cli --no-default-features --features sqlite