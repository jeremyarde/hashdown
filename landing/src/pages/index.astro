---
import { getBaseLandingPageUrl } from "src/lib/utils";

const title = "Hashdown - The Fastest Way to Build Forms";

const faqItems = [
  {
    id: "faq1",
    question: "What is Hashdown?",
    answer:
      "Hashdown is a developer-first form builder. It allows you to create fully functional forms using simple Markdown syntax. It's perfect for developers who prefer typing over dragging and dropping.",
  },
  {
    id: "faq2",
    question: "Do I need coding skills?",
    answer:
      "Not strictly, but being comfortable with Markdown helps. If you can write a README, you can build a form with Hashdown.",
  },
  {
    id: "faq3",
    question: "What kind of forms can I build?",
    answer:
      "You can build surveys, feedback forms, contact forms, and event registrations. We support text inputs, checkboxes, radio buttons, and more.",
  },
  {
    id: "faq4",
    question: "How do I share my forms?",
    answer:
      "Every form gets a unique, secure URL. You can share this link directly or use our embed code to place the form on your own website.",
  },
  {
    id: "faq5",
    question: "Can I embed forms?",
    answer:
      "Absolutely. We provide a simple iframe snippet that you can drop into any website or documentation page.",
  },
  {
    id: "faq6",
    question: "Is there an API?",
    answer:
      "Yes. Our API allows you to programmatically create forms and retrieve responses, enabling deep integration with your applications.",
  },
  {
    id: "faq7",
    question: "Is my data secure?",
    answer:
      "Yes. All data is encrypted in transit and at rest. You retain full ownership of your data and can export it at any time.",
  },
  {
    id: "faq8",
    question: "Can I style my forms?",
    answer:
      "Our default theme is clean and minimal. Pro plans allow for custom CSS injection to match your brand's identity perfectly.",
  },
  {
    id: "faq9",
    question: "Are there limits on forms?",
    answer:
      "The Free plan has generous limits for hobbyists. Paid plans offer unlimited forms and higher response quotas.",
  },
  {
    id: "faq10",
    question: "How does pricing work?",
    answer:
      "We offer a free tier to get started. Our paid plans are simple flat rates designed for power users and teams. See the pricing section for details.",
  },
  {
    id: "faq11",
    question: "Is there team collaboration?",
    answer:
      "Yes, Team plans allow you to invite members to collaborate on forms and analyze responses together.",
  },
  {
    id: "faq12",
    question: "What about support?",
    answer:
      "We offer email support for all users, with priority response times for Pro and Team customers.",
  },
];

const features = [
  {
    title: "Markdown Native",
    description:
      "Write forms as easily as writing a document. Support for all standard inputs.",
    icon: "📝",
  },
  {
    title: "Instant Deployment",
    description:
      "Your form is live the moment you save. Share via link or embed anywhere.",
    icon: "🚀",
  },
  {
    title: "Developer API",
    description:
      "Full API access for programmatic form generation and response handling.",
    icon: "⚡",
  },
];

const tiers = [
  {
    name: "Starter",
    price: "$9.99",
    features: [
      "10 Active Forms",
      "1,000 Responses / mo",
      "Standard Email Support",
      "Email Notifications",
    ],
  },
  {
    name: "Growth",
    price: "$19.99",
    features: [
      "Unlimited Active Forms",
      "10,000 Responses / mo",
      "Priority Email Support",
      "Full API Access",
      "Remove Hashdown Branding",
    ],
  },
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{title}</title>
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <link rel="icon" type="image/png" href="/hashdown.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <nav class="navbar">
      <div class="navbar-content">
        <a class="navbar-logo" href="/">
          <img src="/hashdown.png" alt="Hashdown logo" height="32" />
          <span>Hashdown</span>
        </a>
        <div class="navbar-links">
          <a href="#features">Features</a>
          <a href="editor">Editor</a>
          <!-- <a href="#pricing">Pricing</a> -->
          <!-- <a href="/login" class="nav-login">Log in</a>
          <a href="/signup" class="nav-cta">Sign up</a> -->
        </div>
      </div>
    </nav>

    <main class="container">
      <section class="hero">
        <div class="hero-content">
          <h1 class="hero-title">
            The database for<br />
            <span class="text-gradient">developers</span> who build forms.
          </h1>
          <p class="hero-sub">
            Turn simple Markdown into powerful forms in seconds. No
            drag-and-drop, no complex builders—just text.
          </p>
          <div class="hero-actions">
            <a
              class="cta-btn primary"
              href={getBaseLandingPageUrl() + "/editor"}>Test it now</a
            >
            <a class="cta-btn secondary" href="#features">Read the docs</a>
          </div>
        </div>

        <div class="hero-visual">
          <div class="code-window">
            <div class="code-header">
              <div class="dot dot-red"></div>
              <div class="dot dot-yellow"></div>
              <div class="dot dot-green"></div>
              <div class="window-title">form.md</div>
            </div>
            <div class="code-content">
              <div class="code-line">
                <span class="line-num">1</span><span
                  ><span class="token-hash">#</span>
                  <span class="token-text">Product Feedback</span></span
                >
              </div>
              <div class="code-line">
                <span class="line-num">2</span><span></span>
              </div>
              <div class="code-line">
                <span class="line-num">3</span><span
                  ><span class="token-text">Please rate your experience:</span
                  ></span
                >
              </div>
              <div class="code-line">
                <span class="line-num">4</span><span
                  ><span class="token-field">- [ ]</span>
                  <span class="token-text">Excellent</span></span
                >
              </div>
              <div class="code-line">
                <span class="line-num">5</span><span
                  ><span class="token-field">- [ ]</span>
                  <span class="token-text">Good</span></span
                >
              </div>
              <div class="code-line">
                <span class="line-num">6</span><span
                  ><span class="token-field">- [ ]</span>
                  <span class="token-text">Needs Improvement</span></span
                >
              </div>
              <div class="code-line">
                <span class="line-num">7</span><span></span>
              </div>
              <div class="code-line">
                <span class="line-num">8</span><span
                  ><span class="token-text">Any comments?</span></span
                >
              </div>
              <div class="code-line">
                <span class="line-num">9</span><span
                  ><span class="token-field">___</span></span
                >
              </div>
            </div>
          </div>
        </div>
      </section>

      <section id="features">
        <h2 class="section-title">Why Hashdown?</h2>
        <div class="features-grid">
          {
            features.map((feature) => (
              <div class="feature-card">
                <div class="feature-icon">{feature.icon}</div>
                <h3>{feature.title}</h3>
                <p>{feature.description}</p>
              </div>
            ))
          }
        </div>
      </section>

      <section class="how-it-works">
        <h2 class="section-title">How it works</h2>
        <div class="steps">
          <div class="step">
            <div class="step-number">1</div>
            <h3>Write</h3>
            <p>Define your questions using standard Markdown syntax.</p>
          </div>
          <div class="step">
            <div class="step-arrow">→</div>
          </div>
          <div class="step">
            <div class="step-number">2</div>
            <h3>Share</h3>
            <p>Get a unique URL or embed code for your website instantly.</p>
          </div>
          <div class="step">
            <div class="step-arrow">→</div>
          </div>
          <div class="step">
            <div class="step-number">3</div>
            <h3>Collect</h3>
            <p>View responses in real-time or export them for analysis.</p>
          </div>
        </div>
      </section>

      <section class="content-section">
        <h2 class="section-title">Treat Forms Like Code</h2>
        <p>
          Version control your forms, generate them dynamically, and integrate
          seamlessly with your existing workflow. No more wrestling with clunky
          UI builders.
        </p>
        <div style="text-align: center; margin-top: 2rem">
          <a class="cta-btn primary" href={getBaseLandingPageUrl() + "/editor"}
            >Try it now</a
          >
        </div>
      </section>

      <section id="pricing">
        <h2 class="section-title">Pricing</h2>
        <div class="pricing-page">
          <div class="tiers-container">
            {
              tiers.map((tier) => (
                <div class="tier">
                  <h2 class="tier-name">{tier.name}</h2>
                  <p class="price">
                    {tier.price}
                    <span class="per-month">/month</span>
                  </p>
                  <ul class="features-list-pricing">
                    {tier.features.map((feature) => (
                      <li class="feature">{feature}</li>
                    ))}
                  </ul>
                  <button class="plan-button">Choose Plan</button>
                </div>
              ))
            }
          </div>
        </div>
      </section>

      <section class="faq-section">
        <h2 class="section-title">Questions?</h2>
        <div id="faq-list">
          {
            faqItems.map((faq) => (
              <div class="faq-item" id={faq.id}>
                <div
                  class="faq-question"
                  onclick={`this.parentElement.classList.toggle('open')`}
                >
                  <span>{faq.question}</span>
                  <span class="faq-toggle">+</span>
                </div>
                <div class="faq-answer">{faq.answer}</div>
              </div>
            ))
          }
        </div>
      </section>

      <footer>
        <div class="footer-content">
          <p>© 2025 Hashdown. All rights reserved.</p>
        </div>
      </footer>
    </main>
  </body>
</html>

<style is:global>
  :root {
    --bg-dark: #0b0d11;
    --text-primary: #ededed;
    --text-secondary: #a1a1aa;
    --accent: #fff;
    --accent-hover: #e5e5e5;
    --border-color: #27272a;
    --card-bg: #15171b;
    --grid-color: rgba(255, 255, 255, 0.05);
  }

  html,
  body {
    margin: 0;
    padding: 0;
    font-family: "Inter", system-ui, sans-serif;
    background-color: var(--bg-dark);
    background-image: linear-gradient(var(--grid-color) 1px, transparent 1px),
      linear-gradient(90deg, var(--grid-color) 1px, transparent 1px);
    background-size: 40px 40px;
    color: var(--text-primary);
    min-height: 100vh;
    scroll-behavior: smooth;
  }

  /* Navbar */
  .navbar {
    width: 100%;
    background: rgba(11, 13, 17, 0.8);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border-color);
    display: flex;
    align-items: center;
    justify-content: center;
    position: sticky;
    top: 0;
    z-index: 100;
    height: 64px;
  }
  .navbar-content {
    width: 100%;
    max-width: 1200px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 1.5rem;
  }
  .navbar-logo {
    display: flex;
    align-items: center;
    font-weight: 700;
    font-size: 1.25rem;
    color: var(--text-primary);
    text-decoration: none;
    gap: 0.75rem;
  }
  .navbar-logo img {
    border-radius: 6px;
  }
  .navbar-links {
    display: flex;
    gap: 2rem;
    align-items: center;
  }
  .navbar-links a {
    color: var(--text-secondary);
    text-decoration: none;
    font-weight: 500;
    transition: color 0.2s;
    font-size: 0.9rem;
  }
  .navbar-links a:hover {
    color: var(--text-primary);
  }
  .nav-login {
    margin-left: 1rem;
  }
  .nav-cta {
    background: var(--accent);
    color: #000 !important;
    padding: 0.5rem 1rem;
    border-radius: 4px;
    font-weight: 600;
    transition:
      transform 0.2s,
      background-color 0.2s !important;
  }
  .nav-cta:hover {
    background: var(--accent-hover);
    transform: translateY(-1px);
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
    overflow-x: hidden;
  }

  /* Hero Section */
  .hero {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 8rem 0 6rem;
    gap: 3rem;
  }
  .hero-content {
    max-width: 800px;
  }
  .hero-title {
    font-size: clamp(3rem, 6vw, 4.5rem);
    font-weight: 800;
    color: var(--text-primary);
    margin-bottom: 1.5rem;
    line-height: 1.1;
    letter-spacing: -0.04em;
  }
  .text-gradient {
    background: linear-gradient(120deg, #fff, #999);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  .hero-sub {
    font-size: clamp(1.1rem, 2vw, 1.3rem);
    color: var(--text-secondary);
    margin-bottom: 2.5rem;
    line-height: 1.6;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }
  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
  }
  .cta-btn {
    display: inline-flex;
    align-items: center;
    font-weight: 600;
    font-size: 1rem;
    border: none;
    border-radius: 6px;
    padding: 0.75rem 1.5rem;
    cursor: pointer;
    transition: all 0.2s;
    text-decoration: none;
  }
  .cta-btn.primary {
    background: var(--accent);
    color: #000;
  }
  .cta-btn.primary:hover {
    background: var(--accent-hover);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(255, 255, 255, 0.1);
  }
  .cta-btn.secondary {
    background: transparent;
    color: var(--text-primary);
    border: 1px solid var(--border-color);
  }
  .cta-btn.secondary:hover {
    border-color: var(--text-secondary);
    background: rgba(255, 255, 255, 0.05);
  }

  .hero-visual {
    width: 100%;
    max-width: 800px;
    perspective: 1000px;
    position: relative;
  }
  .hero-visual::before {
    content: "";
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 120%;
    height: 120%;
    background: radial-gradient(
      circle,
      rgba(37, 99, 235, 0.15) 0%,
      transparent 70%
    );
    z-index: -1;
    filter: blur(60px);
    pointer-events: none;
  }

  .code-window {
    background: #0f1115;
    border-radius: 12px;
    padding: 1rem;
    box-shadow:
      0 0 0 1px #333,
      0 20px 50px -10px rgba(0, 0, 0, 0.5);
    border: 1px solid rgba(255, 255, 255, 0.1);
    font-family: "Menlo", "Monaco", "Courier New", monospace;
    overflow: hidden;
    transform: rotateX(2deg);
    transition: transform 0.3s ease;
  }
  .code-window:hover {
    transform: rotateX(0deg) scale(1.01);
  }
  .code-header {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 1rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid #27272a;
  }
  .window-title {
    margin-left: auto;
    margin-right: auto;
    color: var(--text-secondary);
    font-size: 0.8rem;
    transform: translateX(-24px); /* Adjust for dots */
  }
  .dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }
  .dot-red {
    background: #ef4444;
  }
  .dot-yellow {
    background: #f59e0b;
  }
  .dot-green {
    background: #22c55e;
  }

  .code-content {
    color: #e2e8f0;
    font-size: 0.95rem;
    line-height: 1.6;
  }
  .code-line {
    display: flex;
    gap: 1.5rem;
  }
  .line-num {
    color: #4b5563;
    user-select: none;
    width: 20px;
    text-align: right;
  }
  .token-hash {
    color: #60a5fa;
  }
  .token-text {
    color: #f1f5f9;
  }
  .token-field {
    color: #4ade80;
  }

  section {
    margin-bottom: 8rem;
  }
  .section-title {
    font-size: clamp(2rem, 4vw, 2.5rem);
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 4rem;
    text-align: center;
    letter-spacing: -0.02em;
  }

  /* Features */
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }
  .feature-card {
    background: var(--card-bg);
    padding: 2.5rem;
    border-radius: 12px;
    border: 1px solid var(--border-color);
    transition: all 0.3s ease;
  }
  .feature-card:hover {
    border-color: var(--text-secondary);
    transform: translateY(-4px);
  }
  .feature-icon {
    font-size: 2rem;
    margin-bottom: 1.5rem;
    background: rgba(255, 255, 255, 0.05);
    width: 56px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 10px;
  }
  .feature-card h3 {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: 0.75rem;
  }
  .feature-card p {
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* How It Works */
  .how-it-works {
    border-top: 1px solid var(--border-color);
    border-bottom: 1px solid var(--border-color);
    padding: 6rem 0;
    background: linear-gradient(
      to bottom,
      rgba(255, 255, 255, 0.02),
      transparent
    );
  }
  .steps {
    display: flex;
    justify-content: center;
    align-items: flex-start;
    gap: 2rem;
    flex-wrap: wrap;
  }
  .step {
    flex: 1;
    min-width: 250px;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  .step-arrow {
    font-size: 2rem;
    color: var(--text-secondary);
    padding-top: 2rem;
    display: none;
  }
  @media (min-width: 768px) {
    .step-arrow {
      display: block;
    }
  }
  .step-number {
    width: 40px;
    height: 40px;
    background: var(--text-primary);
    color: var(--bg-dark);
    border-radius: 50%;
    font-size: 1.1rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .step h3 {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: 0.75rem;
  }
  .step p {
    color: var(--text-secondary);
    line-height: 1.6;
    max-width: 300px;
  }

  /* Content Section */
  .content-section {
    text-align: center;
    border: 1px solid var(--border-color);
    padding: 4rem 2rem;
    border-radius: 16px;
    background: radial-gradient(
      circle at center,
      rgba(255, 255, 255, 0.05),
      transparent 70%
    );
  }
  .content-section p {
    font-size: 1.2rem;
    color: var(--text-secondary);
    line-height: 1.7;
    max-width: 700px;
    margin: 0 auto;
  }

  /* Pricing */
  .tiers-container {
    display: flex;
    justify-content: center;
    gap: 2rem;
    flex-wrap: wrap;
  }
  .tier {
    background-color: var(--card-bg);
    border: 1px solid var(--border-color);
    border-radius: 12px;
    padding: 2.5rem;
    width: 100%;
    max-width: 350px;
    display: flex;
    flex-direction: column;
    transition: all 0.3s ease;
  }
  .tier:hover {
    border-color: var(--text-primary);
    transform: translateY(-4px);
  }
  .tier-name {
    color: var(--text-primary);
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
  }
  .price {
    font-size: 3rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 1rem 0;
    line-height: 1;
  }
  .per-month {
    font-size: 1rem;
    color: var(--text-secondary);
    font-weight: 400;
  }
  .features-list-pricing {
    list-style-type: none;
    padding: 0;
    margin: 2rem 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .feature {
    padding-left: 1.5rem;
    position: relative;
    color: var(--text-secondary);
  }
  .feature:before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--text-primary);
  }
  .plan-button {
    background: transparent;
    color: var(--text-primary);
    border: 1px solid var(--border-color);
    padding: 0.875rem 2rem;
    font-size: 1rem;
    font-weight: 600;
    border-radius: 6px;
    cursor: pointer;
    transition: all 0.2s ease;
    margin-top: auto;
    width: 100%;
  }
  .plan-button:hover {
    background: var(--text-primary);
    color: var(--bg-dark);
  }

  /* FAQ */
  .faq-section {
    max-width: 800px;
    margin: 0 auto 6rem auto;
  }
  .faq-item {
    border-bottom: 1px solid var(--border-color);
    padding: 1.5rem 0;
  }
  .faq-item:first-child {
    border-top: 1px solid var(--border-color);
  }
  .faq-question {
    font-size: 1.125rem;
    color: var(--text-primary);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: space-between;
    user-select: none;
    font-weight: 500;
  }
  .faq-answer {
    max-height: 0;
    overflow: hidden;
    transition:
      max-height 0.3s ease,
      margin-top 0.3s ease;
    color: var(--text-secondary);
    font-size: 1rem;
    line-height: 1.6;
  }
  .faq-item.open .faq-answer {
    max-height: 500px;
    margin-top: 1rem;
  }
  .faq-toggle {
    color: var(--text-secondary);
    transition: transform 0.3s ease;
    font-size: 1.5rem;
  }
  .faq-item.open .faq-toggle {
    color: var(--text-primary);
    transform: rotate(45deg);
  }

  footer {
    border-top: 1px solid var(--border-color);
    padding: 3rem 0;
    text-align: center;
    color: var(--text-secondary);
    font-size: 0.9rem;
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.5rem;
    }
    .navbar-links {
      display: none; /* Simplified mobile menu */
    }
    .step-arrow {
      display: none;
    }
  }
</style>
