## Install instructions

// Install rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

// Build server
cd server
cargo build
cargo run


#### Generate TS bindings

// from server folder:
cargo test && 

