---
import Layout from "../layouts/Layout.astro";
// import Card from "../components/Card.astro";
import Editor from "../components/Editor";
import Survey from "../components/Survey";

// const wasm = await WebAssembly.instantiateStreaming(
// 	fetch("/markdownparser_bg.wasm")
// );

// const wasm = await WebAssembly.instantiateStreaming(
// 	fetch("../../../backend/pkg")
// );

import { nanoid_gen, parse_markdown_v3 } from "../../../backend/pkg";
import Radio from "../components/Radio";
// let test = initSync();
// init().then((wasmmod) => {
// 	// wasmmod.nanoid_gen(5);
// });
---

<Layout title="Welcome. Create your survey now">
	<main>
		<!-- <Editor parser={parse_markdown_v3} /> -->
		<Survey />
		<!-- <h1>{nanoid_gen(10)}</h1>
		<h1>{JSON.stringify(parse_markdown_v3("- this is a survey"))}</h1> -->
		<Radio />
	</main>
</Layout>
